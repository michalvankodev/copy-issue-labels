on: [pull_request]

jobs:
  copy-labels:
    runs-on: ubuntu-latest
    name: Copy labels from linked issues
    steps:
      - name: copy-labels
        uses: michalvankodev/copy-issue-labels@v1.1.3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          custom-keywords: |
            solves
            implements
